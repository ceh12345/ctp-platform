{
  "name": "ctp-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx concurrently -n api,web -c blue,green \"npm run start:dev --workspace=@ctp/api\" \"npm run dev --workspace=@ctp/web\"",
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
